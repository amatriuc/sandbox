// films table
CREATE TABLE public.films (
    id        serial,
    title       varchar(40) NOT NULL,
    date_prod   varchar(40),
    kind        varchar(40),
    len        	varchar(40),
    note        varchar(40),
    CONSTRAINT pk_films PRIMARY KEY(id)
);
// films table
